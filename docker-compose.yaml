version: '3'
services:
  app:
    image: node:19-alpine
    working_dir: /app
    command: sh -c 'npm install && npm start'
    ports:
      - '27555:3000'
    volumes:
      - ./app:/app
    environment: 
      TZ: 'Asia/Tokyo'
    tty: true
    restart: always